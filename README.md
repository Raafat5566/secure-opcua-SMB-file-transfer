# secure-opcua-SMB-file-transfer
Secure industrial file transfer pipeline using OPC UA and SMB.The system implements a certificate-based OPC UA server (Basic256Sha256, SignAndEncrypt) for secure file ingestion, combined with an automated SMB mover service that validates, transfers, and verifies files into a restricted network share.
